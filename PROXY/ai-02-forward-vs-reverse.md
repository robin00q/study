# 02 · Forward vs Reverse 프록시 — 질문

> 범위: 두 프록시의 방향과 관점 차이. 답은 `02-...md`(내 노트)에.

1. Forward 프록시는 누구 편(클라이언트/서버)에 서서 대리하나? Reverse는?
2. 각각의 경우, 프록시의 존재를 **아는 쪽**은 누구인가? (클라이언트가 아나, 서버가 아나)
3. Forward 프록시의 대표 사용 사례 3가지?
4. Reverse 프록시의 대표 사용 사례 3가지? (로드밸런싱 / TLS 종료 / 캐싱 …)
5. 회사 방화벽의 "아웃바운드 프록시"는 forward인가 reverse인가? 웹서비스 앞단 nginx는?
6. 같은 소프트웨어(nginx 등)가 forward도 reverse도 될 수 있나? 무엇으로 결정되나?
7. Transparent(투명) 프록시는 일반 forward와 무엇이 다른가? (클라이언트 설정 유무)
8. Authkey에서 쓴 pod 프록시는 forward인가 reverse인가? 왜?
