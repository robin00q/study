# 04 · SOCKS 프록시 (SOCKS5 · ssh -D) — 질문

> 범위: SOCKS의 계층·범용성, HTTP 프록시와의 차이. 답은 `04-...md`(내 노트)에.

1. SOCKS는 어느 계층에서 동작하나? HTTP 프록시(응용 계층)와의 근본 차이는?
2. SOCKS4 vs SOCKS5의 차이를 3가지 이상 들면? (인증, UDP, 도메인 해석 …)
3. "SOCKS5는 DNS 조회를 어디서 하나"(로컬 vs 원격)? 왜 이게 중요한가? (프라이버시/도달성)
4. `ssh -D 1080 user@host` 명령이 만들어내는 것은 무엇인가? 서버 쪽에는 무엇이 설치돼 있어야 하나?
5. SOCKS 프록시가 HTTP 프록시보다 "범용"인 이유는? (어떤 트래픽까지 태울 수 있나)
6. 브라우저/앱/`curl`에 SOCKS 프록시를 어떻게 지정하나?
7. Authkey 사례에서 HTTP 프록시가 아니라 SOCKS(`ssh -D` / microsocks)를 택한 이유를 추론하면?
