name: cdc-lab

# CDC end-to-end 랩 (Debezium 공식 튜토리얼 구성)
# [mysql] -> (connect = Kafka Connect + Debezium) -> [kafka]
services:
  zookeeper:
    image: quay.io/debezium/zookeeper:2.7
    ports:
      - "2181:2181"

  kafka:
    image: quay.io/debezium/kafka:2.7
    ports:
      - "9092:9092"
    depends_on:
      - zookeeper
    environment:
      ZOOKEEPER_CONNECT: zookeeper:2181

  mysql:
    image: quay.io/debezium/example-mysql:2.7   # binlog 켜짐 + inventory DB 내장 + debezium 유저
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: debezium
      MYSQL_USER: mysqluser
      MYSQL_PASSWORD: mysqlpw

  connect:
    image: quay.io/debezium/connect:2.7
    ports:
      - "8083:8083"
    depends_on:
      - kafka
      - mysql
    environment:
      BOOTSTRAP_SERVERS: kafka:9092
      GROUP_ID: "1"
      CONFIG_STORAGE_TOPIC: my_connect_configs
      OFFSET_STORAGE_TOPIC: my_connect_offsets    # ← 03/04에서 배운 커넥터 offset 저장 토픽
      STATUS_STORAGE_TOPIC: my_connect_statuses
  console:
    image: docker.redpanda.com/redpandadata/console:latest   # 카프카/커넥터 관찰용 웹 UI (http://localhost:8085)
    ports:
      - "8085:8080"
    depends_on:
      - kafka
      - connect
    environment:
      KAFKA_BROKERS: kafka:9092
      CONNECT_ENABLED: "true"          # Kafka Connect 연동 (커넥터 UI 조회)
      CONNECT_CLUSTERS_0_NAME: cdc-lab
      CONNECT_CLUSTERS_0_URL: http://connect:8083
